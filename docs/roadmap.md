# CGUL Roadmap

## v0.1

- Canonical spec seed (`docs/spec.md`)
- `.cgul` format definition (`docs/format_cgul.md`)
- Core IO and validation
- CLI save/load + terminal composition

## v0.2

- Demo app generator/editor
- Deterministic `--seed` workflows
- Save/load round-trip UX improvements

## v0.3

- SFML adapter for glyph-grid rendering and input mapping

## v0.4

- Snapshot/export tools for regression and inspection

## v0.5

- Godot adapter path (JSON import/render first, then extension path)
